import React from 'react';

export default function Settings() {
  return (
    <div>
      <h2>Settings</h2>
      <p>Adjust correlation settings in the backend or Streamlit UI. React version uses API defaults.</p>
    </div>
  );
}
